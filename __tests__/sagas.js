import 'react-native';
import React from 'react';
import { loadDirectory } from '../js/sagas.js';

//TODO
it('does some stuff', () => {
  loadDirectory(loadDirectory( curren
});
it('renders correctly', () => {
  const tree = renderer.create(
    <Index />
  );
});
